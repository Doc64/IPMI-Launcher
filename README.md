# IPMI-Launcher
Takes an old JNLP file from older AMI BMCs to allow old java based IPMI to launch on modern machines without requiring the need to install an older java version. This app packages a portable java 7 installation that directly launches the file.
